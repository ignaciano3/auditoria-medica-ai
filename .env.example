DATABASE_URL=postgres://audit:audit@localhost:5432/audit
S3_ENDPOINT=http://localhost:9000
S3_BUCKET=documents
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
LLM_PROVIDER=openai
LLM_MODEL=gpt-4.1
# OCR (Optical Character Recognition)
# openai = vision model for classify + transcribe | tesseract = tesseract binary for transcribe + vision model only for classify
OCR_PROVIDER=openai
OCR_MODEL=gpt-4.1
OPENAI_API_KEY=sk-replace-me
DOCUMENT_RETENTION_DAYS=30
